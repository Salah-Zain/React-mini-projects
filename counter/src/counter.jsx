import React from 'react'

const Counter = (props) => {
  return (
    
    <div>
          <h1 style={{color:'whitesmoke',fontSize:'48px',textAlign:'center'}}>{props.count}</h1>

    </div>
  )
}

export default Counter
